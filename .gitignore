# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/

# Virtual environments (local test fallback; canonical env is Nix)
.venv/
venv/

# Nix
result
result-*
.direnv/

# Local tooling (uv, standalone pythons)
tools/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Local machine configuration (copy .env.example to .env)
.env

# uv lockfile from local test installs (canonical env is Nix)
uv.lock

# Assembled dictionaries are release artifacts, never sources (spec §13)
output/
releases/
# Release notes generated by scripts/scope_info.py (see docs/workflow.md)
/scope.md
