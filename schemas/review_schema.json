{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://cfdict-next.example/schemas/review_schema.json",
    "title": "Review LLM Dictionary",
    "description": "Schema for review.json. The record shape is defined once in llm_entry.json (the single normative source); this file only pins confidence to 'review'.",
    "type": "object",
    "additionalProperties": {
        "allOf": [
            { "$ref": "llm_entry.json" },
            { "properties": { "confidence": { "const": "review" } } }
        ]
    }
}
