{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://cfdict-next.example/schemas/confident_schema.json",
    "title": "Confident LLM Dictionary",
    "description": "Schema for confident.json. The record shape is defined once in llm_entry.json (the single normative source); this file only pins confidence to 'confident'.",
    "type": "object",
    "additionalProperties": {
        "allOf": [
            { "$ref": "llm_entry.json" },
            { "properties": { "confidence": { "const": "confident" } } }
        ]
    }
}
